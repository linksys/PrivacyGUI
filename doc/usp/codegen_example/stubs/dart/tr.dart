/// Minimal tr() stub for codegen examples.
/// In production, replace this import with your actual i18n library.
///
/// Example with easy_localization:
///   import 'package:easy_localization/easy_localization.dart';
///
/// Example with flutter_localizations:
///   String tr(String key) => AppLocalizations.of(context)!.translate(key);

String tr(String key) => key;
