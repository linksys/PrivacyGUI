// AUTO-GENERATED CODE - DO NOT EDIT
// This file was generated by usp-codegen
// Any modifications will be overwritten on next generation

import 'package:privacy_gui/usp/services/usp_service.dart';

/// Single instance from VendorLogFiles
class VendorLogFile {
  final String instancePath;
  final String name;
  final int maximumSize;
  final bool persistent;

  const VendorLogFile({
    required this.instancePath,
    required this.name,
    required this.maximumSize,
    required this.persistent,
  });
}

/// Router vendor log files (multi-instance, read-only metadata)
class VendorLogFiles {
  final List<VendorLogFile> items;

  const VendorLogFiles({required this.items});

  static const _paths = [
    'Device.DeviceInfo.VendorLogFile.*.Name',
    'Device.DeviceInfo.VendorLogFile.*.MaximumSize',
    'Device.DeviceInfo.VendorLogFile.*.Persistent',
  ];

  /// Fetch all instances via USP Get message
  static Future<VendorLogFiles> fetch(UspService client) async {
    final response = await client.get(_paths);
    return VendorLogFiles._fromResponse(response);
  }

  factory VendorLogFiles._fromResponse(Map<String, dynamic> response) {
    final items = <VendorLogFile>[];
    const basePath = 'Device.DeviceInfo.VendorLogFile.';
    final ids = <String>{};
    for (final key in response.keys) {
      if (key.startsWith(basePath)) {
        final rest = key.substring(basePath.length);
        final dot = rest.indexOf('.');
        if (dot > 0) ids.add(rest.substring(0, dot));
      }
    }
    final sorted = ids.toList()
      ..sort((a, b) => (int.tryParse(a) ?? 0).compareTo(int.tryParse(b) ?? 0));
    for (final id in sorted) {
      final p = '$basePath$id.';
      if ([
        response['${p}Name'],
        response['${p}MaximumSize'],
        response['${p}Persistent']
      ].every((v) =>
          v == null ||
          v == '' ||
          v == '0' ||
          v == 0 ||
          v == false ||
          v == 'false')) continue;
      items.add(VendorLogFile(
        instancePath: p,
        name: (response['${p}Name'] ?? '') as String,
        maximumSize:
            int.tryParse(response['${p}MaximumSize']?.toString() ?? '') ?? 0,
        persistent: response['${p}Persistent'] == true ||
            response['${p}Persistent'] == 'true' ||
            response['${p}Persistent'] == '1',
      ));
    }
    return VendorLogFiles(items: items);
  }
}
