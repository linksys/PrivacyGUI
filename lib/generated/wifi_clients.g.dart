// AUTO-GENERATED CODE - DO NOT EDIT
// This file was generated by usp-codegen
// Any modifications will be overwritten on next generation


import 'package:privacy_gui/usp/services/usp_service.dart';

/// Flattened instance from nested multi-instance table
class WifiClient {
  final String instancePath;
  final String parentPath;
  final String macAddress;
  final int signalStrength;
  final int noise;
  final int lastDataDownlinkRate;
  final int lastDataUplinkRate;
  final bool active;

  const WifiClient({
    required this.instancePath,
    required this.parentPath,
    required this.macAddress,
    required this.signalStrength,
    required this.noise,
    required this.lastDataDownlinkRate,
    required this.lastDataUplinkRate,
    required this.active,
  });
}

/// WiFi clients across all access points (flattened view)
class WifiClients {
  final List<WifiClient> items;

  const WifiClients({required this.items});

  static const _paths = ['Device.WiFi.AccessPoint.'];

  /// Fetch all instances via USP Get message
  static Future<WifiClients> fetch(UspService client) async {
    final response = await client.get(_paths);
    return WifiClients._fromResponse(response);
  }

  factory WifiClients._fromResponse(Map<String, dynamic> response) {
    final items = <WifiClient>[];
    const basePath = 'Device.WiFi.AccessPoint.';
    final parentIds = <String>{};
    for (final key in response.keys) {
      if (key.startsWith(basePath)) {
        final rest = key.substring(basePath.length);
        final dot = rest.indexOf('.');
        if (dot > 0) parentIds.add(rest.substring(0, dot));
      }
    }
    final sortedParents = parentIds.toList()..sort((a, b) =>
        (int.tryParse(a) ?? 0).compareTo(int.tryParse(b) ?? 0));
    for (final pid in sortedParents) {
      final parentPath = '$basePath$pid.';
      final childBase = '${parentPath}AssociatedDevice.';
      final childIds = <String>{};
      for (final key in response.keys) {
        if (key.startsWith(childBase)) {
          final rest = key.substring(childBase.length);
          final dot = rest.indexOf('.');
          if (dot > 0) childIds.add(rest.substring(0, dot));
        }
      }
      final sortedChildren = childIds.toList()..sort((a, b) =>
          (int.tryParse(a) ?? 0).compareTo(int.tryParse(b) ?? 0));
      for (final cid in sortedChildren) {
        final cp = '$childBase$cid.';
        items.add(WifiClient(
          instancePath: cp,
          parentPath: parentPath,
          macAddress: (response['${cp}MACAddress'] ?? '') as String,
          signalStrength: int.tryParse(response['${cp}SignalStrength']?.toString() ?? '') ?? 0,
          noise: int.tryParse(response['${cp}Noise']?.toString() ?? '') ?? 0,
          lastDataDownlinkRate: int.tryParse(response['${cp}LastDataDownlinkRate']?.toString() ?? '') ?? 0,
          lastDataUplinkRate: int.tryParse(response['${cp}LastDataUplinkRate']?.toString() ?? '') ?? 0,
          active: response['${cp}Active'] == true || response['${cp}Active'] == 'true',
        ));
      }
    }
    return WifiClients(items: items);
  }

}
