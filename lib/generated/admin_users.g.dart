// AUTO-GENERATED CODE - DO NOT EDIT
// This file was generated by usp-codegen
// Any modifications will be overwritten on next generation


import 'package:privacy_gui/usp/services/usp_service.dart';

/// Single instance from AdminUsers
class AdminUser {
  final String instancePath;
  final String username;
  final String password;
  final bool enable;

  const AdminUser({
    required this.instancePath,
    required this.username,
    required this.password,
    required this.enable,
  });
}

/// Update descriptor for AdminUser instances
class AdminUserUpdate {
  final String instancePath;
  final String? password;

  const AdminUserUpdate({
    required this.instancePath,
    this.password,
  });
}

/// Router admin user accounts (multi-instance)
class AdminUsers {
  final List<AdminUser> items;

  const AdminUsers({required this.items});

  static const _paths = [
    'Device.Users.User.*.Username',
    'Device.Users.User.*.Password',
    'Device.Users.User.*.Enable',
  ];

  /// Fetch all instances via USP Get message
  static Future<AdminUsers> fetch(UspService client) async {
    final response = await client.get(_paths);
    return AdminUsers._fromResponse(response);
  }

  factory AdminUsers._fromResponse(Map<String, dynamic> response) {
    final items = <AdminUser>[];
    const basePath = 'Device.Users.User.';
    final ids = <String>{};
    for (final key in response.keys) {
      if (key.startsWith(basePath)) {
        final rest = key.substring(basePath.length);
        final dot = rest.indexOf('.');
        if (dot > 0) ids.add(rest.substring(0, dot));
      }
    }
    final sorted = ids.toList()..sort((a, b) =>
        (int.tryParse(a) ?? 0).compareTo(int.tryParse(b) ?? 0));
    for (final id in sorted) {
      final p = '$basePath$id.';
      if ([response['${p}Username'], response['${p}Password'], response['${p}Enable']].every((v) => v == null || v == '' || v == '0' || v == 0 || v == false || v == 'false')) continue;
      items.add(AdminUser(
        instancePath: p,
        username: (response['${p}Username'] ?? '') as String,
        password: (response['${p}Password'] ?? '') as String,
        enable: response['${p}Enable'] == true || response['${p}Enable'] == 'true' || response['${p}Enable'] == '1',
      ));
    }
    return AdminUsers(items: items);
  }

  /// Update a single instance via USP Set message
  static Future<void> update(UspService client, AdminUserUpdate update) async {
    final params = <String, dynamic>{};
    if (update.password != null) params['${update.instancePath}Password'] = update.password;
    if (params.isNotEmpty) await client.set(params);
  }

  /// Update multiple instances in a single USP Set message
  static Future<void> updateMany(UspService client, List<AdminUserUpdate> updates, {bool allowPartial = false}) async {
    final params = <String, dynamic>{};
    for (final update in updates) {
      if (update.password != null) params['${update.instancePath}Password'] = update.password;
    }
    if (params.isNotEmpty) await client.set(params, allowPartial: allowPartial);
  }

}
