// AUTO-GENERATED CODE - DO NOT EDIT
// This file was generated by usp-codegen
// Any modifications will be overwritten on next generation

import 'package:privacy_gui/core/usp/services/usp_client.dart';

/// Single instance from VlanTermination
class VlanTerminationInstance {
  final String instancePath;
  final bool enable;
  final int vlanId;

  const VlanTerminationInstance({
    required this.instancePath,
    required this.enable,
    required this.vlanId,
  });
}

/// Update descriptor for VlanTerminationInstance instances
class VlanTerminationInstanceUpdate {
  final String instancePath;
  final bool? enable;
  final int? vlanId;

  const VlanTerminationInstanceUpdate({
    required this.instancePath,
    this.enable,
    this.vlanId,
  });
}

/// VLAN termination instances for WAN VLAN tagging
class VlanTermination {
  final List<VlanTerminationInstance> items;

  const VlanTermination({required this.items});

  static const _paths = [
    'Device.Ethernet.VLANTermination.*.Enable',
    'Device.Ethernet.VLANTermination.*.VLANID',
  ];

  /// Fetch all instances via USP Get message
  static Future<VlanTermination> fetch(UspClient client) async {
    final response = await client.get(_paths);
    return VlanTermination._fromResponse(response);
  }

  factory VlanTermination._fromResponse(Map<String, dynamic> response) {
    final items = <VlanTerminationInstance>[];
    const basePath = 'Device.Ethernet.VLANTermination.';
    final ids = <String>{};
    for (final key in response.keys) {
      if (key.startsWith(basePath)) {
        final rest = key.substring(basePath.length);
        final dot = rest.indexOf('.');
        if (dot > 0) ids.add(rest.substring(0, dot));
      }
    }
    final sorted = ids.toList()
      ..sort((a, b) => (int.tryParse(a) ?? 0).compareTo(int.tryParse(b) ?? 0));
    for (final id in sorted) {
      final p = '$basePath$id.';
      if ([response['${p}Enable'], response['${p}VLANID']].every((v) =>
          v == null ||
          v == '' ||
          v == '0' ||
          v == 0 ||
          v == false ||
          v == 'false')) continue;
      items.add(VlanTerminationInstance(
        instancePath: p,
        enable: response['${p}Enable'] == true ||
            response['${p}Enable'] == 'true' ||
            response['${p}Enable'] == '1',
        vlanId: int.tryParse(response['${p}VLANID']?.toString() ?? '') ?? 0,
      ));
    }
    return VlanTermination(items: items);
  }

  /// Update a single instance via USP Set message
  static Future<Map<String, dynamic>> update(
      UspClient client, VlanTerminationInstanceUpdate update) async {
    final params = <String, dynamic>{};
    if (update.enable != null)
      params['${update.instancePath}Enable'] = update.enable;
    if (update.vlanId != null)
      params['${update.instancePath}VLANID'] = update.vlanId;
    if (params.isEmpty) {
      return {
        'overallSuccess': true,
        'hasAnySuccess': false,
        'hasErrors': false,
        'results': []
      };
    }
    return await client.set(params);
  }

  /// Update multiple instances in a single USP Set message
  static Future<Map<String, dynamic>> updateMany(
      UspClient client, List<VlanTerminationInstanceUpdate> updates,
      {bool allowPartial = false}) async {
    final params = <String, dynamic>{};
    for (final update in updates) {
      if (update.enable != null)
        params['${update.instancePath}Enable'] = update.enable;
      if (update.vlanId != null)
        params['${update.instancePath}VLANID'] = update.vlanId;
    }
    if (params.isEmpty) {
      return {
        'overallSuccess': true,
        'hasAnySuccess': false,
        'hasErrors': false,
        'results': []
      };
    }
    return await client.set(params, allowPartial: allowPartial);
  }

  /// Add a new VlanTerminationInstance instance via USP Add message
  static Future<Map<String, dynamic>> add(
    UspClient client, {
    bool? enable,
    int? vlanId,
  }) async {
    final params = <String, dynamic>{};
    if (enable != null) params['Enable'] = enable;
    if (vlanId != null) params['VLANID'] = vlanId;
    return await client.add('Device.Ethernet.VLANTermination.', params);
  }

  /// Delete a VlanTerminationInstance instance via USP Delete message
  static Future<Map<String, dynamic>> delete(
      UspClient client, String instancePath) async {
    return await client.delete(instancePath);
  }
}
