// AUTO-GENERATED CODE - DO NOT EDIT
// This file was generated by usp-codegen
// Any modifications will be overwritten on next generation

import 'package:privacy_gui/core/usp/services/usp_client.dart';

/// Single instance from Dhcpv4ServerPools
class Dhcpv4ServerPool {
  final String instancePath;
  final String? alias;
  final bool enable;
  final String minAddress;
  final String maxAddress;
  final int leaseTime;
  final String? interface_;

  const Dhcpv4ServerPool({
    required this.instancePath,
    this.alias,
    required this.enable,
    required this.minAddress,
    required this.maxAddress,
    required this.leaseTime,
    this.interface_,
  });
}

/// Update descriptor for Dhcpv4ServerPool instances
class Dhcpv4ServerPoolUpdate {
  final String instancePath;
  final bool? enable;
  final String? minAddress;
  final String? maxAddress;
  final int? leaseTime;

  const Dhcpv4ServerPoolUpdate({
    required this.instancePath,
    this.enable,
    this.minAddress,
    this.maxAddress,
    this.leaseTime,
  });
}

/// DHCP server pools with Alias for lan/guest discovery
class Dhcpv4ServerPools {
  final List<Dhcpv4ServerPool> items;

  const Dhcpv4ServerPools({required this.items});

  static const _paths = [
    'Device.DHCPv4.Server.Pool.*.Alias',
    'Device.DHCPv4.Server.Pool.*.Enable',
    'Device.DHCPv4.Server.Pool.*.MinAddress',
    'Device.DHCPv4.Server.Pool.*.MaxAddress',
    'Device.DHCPv4.Server.Pool.*.LeaseTime',
    'Device.DHCPv4.Server.Pool.*.Interface',
  ];

  /// Fetch all instances via USP Get message
  static Future<Dhcpv4ServerPools> fetch(UspClient client) async {
    final response = await client.get(_paths);
    return Dhcpv4ServerPools._fromResponse(response);
  }

  factory Dhcpv4ServerPools._fromResponse(Map<String, dynamic> response) {
    final items = <Dhcpv4ServerPool>[];
    const basePath = 'Device.DHCPv4.Server.Pool.';
    final ids = <String>{};
    for (final key in response.keys) {
      if (key.startsWith(basePath)) {
        final rest = key.substring(basePath.length);
        final dot = rest.indexOf('.');
        if (dot > 0) ids.add(rest.substring(0, dot));
      }
    }
    final sorted = ids.toList()
      ..sort((a, b) => (int.tryParse(a) ?? 0).compareTo(int.tryParse(b) ?? 0));
    for (final id in sorted) {
      final p = '$basePath$id.';
      if ([
        response['${p}Alias'],
        response['${p}Enable'],
        response['${p}MinAddress'],
        response['${p}MaxAddress'],
        response['${p}LeaseTime'],
        response['${p}Interface']
      ].every((v) =>
          v == null ||
          v == '' ||
          v == '0' ||
          v == 0 ||
          v == false ||
          v == 'false')) {
        continue;
      }
      final missing = <String>[];
      if (!response.containsKey('${p}Enable')) {
        missing.add('${p}Enable');
      }
      if (!response.containsKey('${p}MinAddress')) {
        missing.add('${p}MinAddress');
      }
      if (!response.containsKey('${p}MaxAddress')) {
        missing.add('${p}MaxAddress');
      }
      if (!response.containsKey('${p}LeaseTime')) {
        missing.add('${p}LeaseTime');
      }
      if (missing.isNotEmpty) {
        throw 'Get failed: Validation error: Required fields missing from response: ${missing.join(", ")} (code: 9998)';
      }
      items.add(Dhcpv4ServerPool(
        instancePath: p,
        alias: response.containsKey('${p}Alias')
            ? response['${p}Alias'] as String
            : null,
        enable: response['${p}Enable'] == true ||
            response['${p}Enable'] == 'true' ||
            response['${p}Enable'] == '1',
        minAddress: (response['${p}MinAddress'] ?? '') as String,
        maxAddress: (response['${p}MaxAddress'] ?? '') as String,
        leaseTime:
            int.tryParse(response['${p}LeaseTime']?.toString() ?? '') ?? 0,
        interface_: response.containsKey('${p}Interface')
            ? response['${p}Interface'] as String
            : null,
      ));
    }
    return Dhcpv4ServerPools(items: items);
  }

  /// Update writable parameters via USP Set message
  static Future<Map<String, dynamic>> update(
      UspClient client, List<Dhcpv4ServerPoolUpdate> updates,
      {bool allowPartial = false}) async {
    final params = <String, dynamic>{};
    for (final update in updates) {
      if (update.enable != null) {
        params['${update.instancePath}Enable'] = update.enable;
      }
      if (update.minAddress != null) {
        params['${update.instancePath}MinAddress'] = update.minAddress;
      }
      if (update.maxAddress != null) {
        params['${update.instancePath}MaxAddress'] = update.maxAddress;
      }
      if (update.leaseTime != null) {
        params['${update.instancePath}LeaseTime'] = update.leaseTime;
      }
    }
    if (params.isEmpty) {
      return {
        'success': true,
        'result': {'data': <String, dynamic>{}}
      };
    }
    return await client.set(params, allowPartial: allowPartial);
  }
}
