// AUTO-GENERATED CODE - DO NOT EDIT
// This file was generated by usp-codegen
// Any modifications will be overwritten on next generation

import 'package:privacy_gui/core/usp/services/usp_client.dart';

/// Single instance from FirmwareImages
class FirmwareImage {
  final String instancePath;
  final String name;
  final String version;
  final String status;
  final bool available;

  const FirmwareImage({
    required this.instancePath,
    required this.name,
    required this.version,
    required this.status,
    required this.available,
  });
}

/// Firmware image partitions (dual image status)
class FirmwareImages {
  final List<FirmwareImage> items;

  const FirmwareImages({required this.items});

  static const _paths = [
    'Device.DeviceInfo.FirmwareImage.*.Name',
    'Device.DeviceInfo.FirmwareImage.*.Version',
    'Device.DeviceInfo.FirmwareImage.*.Status',
    'Device.DeviceInfo.FirmwareImage.*.Available',
  ];

  /// Fetch all instances via USP Get message
  static Future<FirmwareImages> fetch(UspClient client) async {
    final response = await client.get(_paths);
    return FirmwareImages._fromResponse(response);
  }

  factory FirmwareImages._fromResponse(Map<String, dynamic> response) {
    final items = <FirmwareImage>[];
    const basePath = 'Device.DeviceInfo.FirmwareImage.';
    final ids = <String>{};
    for (final key in response.keys) {
      if (key.startsWith(basePath)) {
        final rest = key.substring(basePath.length);
        final dot = rest.indexOf('.');
        if (dot > 0) ids.add(rest.substring(0, dot));
      }
    }
    final sorted = ids.toList()
      ..sort((a, b) => (int.tryParse(a) ?? 0).compareTo(int.tryParse(b) ?? 0));
    for (final id in sorted) {
      final p = '$basePath$id.';
      if ([
        response['${p}Name'],
        response['${p}Version'],
        response['${p}Status'],
        response['${p}Available']
      ].every((v) =>
          v == null ||
          v == '' ||
          v == '0' ||
          v == 0 ||
          v == false ||
          v == 'false')) continue;
      final missing = <String>[];
      if (!response.containsKey('${p}Name')) missing.add('${p}Name');
      if (!response.containsKey('${p}Version')) missing.add('${p}Version');
      if (!response.containsKey('${p}Status')) missing.add('${p}Status');
      if (!response.containsKey('${p}Available')) missing.add('${p}Available');
      if (missing.isNotEmpty) {
        throw Exception(
            '{errorCode: 9998, errorMessage: "Required fields missing from response: ${missing.join(", ")}"}');
      }
      items.add(FirmwareImage(
        instancePath: p,
        name: (response['${p}Name'] ?? '') as String,
        version: (response['${p}Version'] ?? '') as String,
        status: (response['${p}Status'] ?? '') as String,
        available: response['${p}Available'] == true ||
            response['${p}Available'] == 'true' ||
            response['${p}Available'] == '1',
      ));
    }
    return FirmwareImages(items: items);
  }
}
