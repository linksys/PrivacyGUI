// AUTO-GENERATED CODE - DO NOT EDIT
// This file was generated by usp-codegen
// Any modifications will be overwritten on next generation

import 'package:privacy_gui/core/usp/services/usp_client.dart';

/// Single instance from EthernetLinks
class EthernetLink {
  final String instancePath;
  final String? alias;
  final String macAddress;
  final String? name;
  final String? status;
  final String? lowerLayers;

  const EthernetLink({
    required this.instancePath,
    this.alias,
    required this.macAddress,
    this.name,
    this.status,
    this.lowerLayers,
  });
}

/// Update descriptor for EthernetLink instances
class EthernetLinkUpdate {
  final String instancePath;
  final String? macAddress;

  const EthernetLinkUpdate({
    required this.instancePath,
    this.macAddress,
  });
}

/// Ethernet link layer instances for MAC clone
class EthernetLinks {
  final List<EthernetLink> items;

  const EthernetLinks({required this.items});

  static const _paths = [
    'Device.Ethernet.Link.*.Alias',
    'Device.Ethernet.Link.*.MACAddress',
    'Device.Ethernet.Link.*.Name',
    'Device.Ethernet.Link.*.Status',
    'Device.Ethernet.Link.*.LowerLayers',
  ];

  /// Fetch all instances via USP Get message
  static Future<EthernetLinks> fetch(UspClient client) async {
    final response = await client.get(_paths);
    return EthernetLinks._fromResponse(response);
  }

  factory EthernetLinks._fromResponse(Map<String, dynamic> response) {
    final items = <EthernetLink>[];
    const basePath = 'Device.Ethernet.Link.';
    final ids = <String>{};
    for (final key in response.keys) {
      if (key.startsWith(basePath)) {
        final rest = key.substring(basePath.length);
        final dot = rest.indexOf('.');
        if (dot > 0) ids.add(rest.substring(0, dot));
      }
    }
    final sorted = ids.toList()
      ..sort((a, b) => (int.tryParse(a) ?? 0).compareTo(int.tryParse(b) ?? 0));
    for (final id in sorted) {
      final p = '$basePath$id.';
      if ([
        response['${p}Alias'],
        response['${p}MACAddress'],
        response['${p}Name'],
        response['${p}Status'],
        response['${p}LowerLayers']
      ].every((v) =>
          v == null ||
          v == '' ||
          v == '0' ||
          v == 0 ||
          v == false ||
          v == 'false')) {
        continue;
      }
      final missing = <String>[];
      if (!response.containsKey('${p}MACAddress')) {
        missing.add('${p}MACAddress');
      }
      if (missing.isNotEmpty) {
        throw 'Get failed: Validation error: Required fields missing from response: ${missing.join(", ")} (code: 9998)';
      }
      items.add(EthernetLink(
        instancePath: p,
        alias: response.containsKey('${p}Alias')
            ? response['${p}Alias'] as String
            : null,
        macAddress: (response['${p}MACAddress'] ?? '') as String,
        name: response.containsKey('${p}Name')
            ? response['${p}Name'] as String
            : null,
        status: response.containsKey('${p}Status')
            ? response['${p}Status'] as String
            : null,
        lowerLayers: response.containsKey('${p}LowerLayers')
            ? response['${p}LowerLayers'] as String
            : null,
      ));
    }
    return EthernetLinks(items: items);
  }

  /// Update writable parameters via USP Set message
  static Future<Map<String, dynamic>> update(
      UspClient client, List<EthernetLinkUpdate> updates,
      {bool allowPartial = false}) async {
    final params = <String, dynamic>{};
    for (final update in updates) {
      if (update.macAddress != null) {
        params['${update.instancePath}MACAddress'] = update.macAddress;
      }
    }
    if (params.isEmpty) {
      return {
        'success': true,
        'result': {'data': <String, dynamic>{}}
      };
    }
    return await client.set(params, allowPartial: allowPartial);
  }
}
