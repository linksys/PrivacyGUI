// AUTO-GENERATED CODE - DO NOT EDIT
// This file was generated by usp-codegen
// Any modifications will be overwritten on next generation

import 'package:privacy_gui/usp/services/usp_service.dart';

/// Nested instance from parent multi-instance table
class PortTriggerForwardRule {
  final String instancePath;
  final int forwardPort;
  final int forwardPortEndRange;
  final String forwardProtocol;

  const PortTriggerForwardRule({
    required this.instancePath,
    required this.forwardPort,
    required this.forwardPortEndRange,
    required this.forwardProtocol,
  });
}

/// Single instance from PortTriggering
class PortTrigger {
  final String instancePath;
  final bool enabled;
  final String description;
  final int triggerPort;
  final int triggerPortEndRange;
  final String triggerProtocol;
  final List<PortTriggerForwardRule> rules;

  const PortTrigger({
    required this.instancePath,
    required this.enabled,
    required this.description,
    required this.triggerPort,
    required this.triggerPortEndRange,
    required this.triggerProtocol,
    required this.rules,
  });
}

/// Update descriptor for PortTrigger instances
class PortTriggerUpdate {
  final String instancePath;
  final bool? enabled;
  final String? description;
  final int? triggerPort;
  final int? triggerPortEndRange;
  final String? triggerProtocol;

  const PortTriggerUpdate({
    required this.instancePath,
    this.enabled,
    this.description,
    this.triggerPort,
    this.triggerPortEndRange,
    this.triggerProtocol,
  });
}

/// NAT port triggering rules with forwarded port sub-rules
class PortTriggering {
  final List<PortTrigger> items;

  const PortTriggering({required this.items});

  static const _paths = ['Device.NAT.PortTrigger.'];

  /// Fetch all instances via USP Get message
  static Future<PortTriggering> fetch(UspService client) async {
    final response = await client.get(_paths);
    return PortTriggering._fromResponse(response);
  }

  factory PortTriggering._fromResponse(Map<String, dynamic> response) {
    final items = <PortTrigger>[];
    const basePath = 'Device.NAT.PortTrigger.';
    final ids = <String>{};
    for (final key in response.keys) {
      if (key.startsWith(basePath)) {
        final rest = key.substring(basePath.length);
        final dot = rest.indexOf('.');
        if (dot > 0) ids.add(rest.substring(0, dot));
      }
    }
    final sorted = ids.toList()
      ..sort((a, b) => (int.tryParse(a) ?? 0).compareTo(int.tryParse(b) ?? 0));
    for (final id in sorted) {
      final p = '$basePath$id.';
      final childBase_0 = '${p}Rule.';
      final childIds_0 = <String>{};
      for (final key in response.keys) {
        if (key.startsWith(childBase_0)) {
          final rest = key.substring(childBase_0.length);
          final dot = rest.indexOf('.');
          if (dot > 0) childIds_0.add(rest.substring(0, dot));
        }
      }
      final childSorted_0 = childIds_0.toList()
        ..sort(
            (a, b) => (int.tryParse(a) ?? 0).compareTo(int.tryParse(b) ?? 0));
      final rules = <PortTriggerForwardRule>[];
      for (final cid_0 in childSorted_0) {
        final cp_0 = '${childBase_0}${cid_0}.';
        rules.add(PortTriggerForwardRule(
          instancePath: cp_0,
          forwardPort:
              int.tryParse(response['${cp_0}Port']?.toString() ?? '') ?? 0,
          forwardPortEndRange:
              int.tryParse(response['${cp_0}PortEndRange']?.toString() ?? '') ??
                  0,
          forwardProtocol: (response['${cp_0}Protocol'] ?? '') as String,
        ));
      }
      items.add(PortTrigger(
        instancePath: p,
        enabled:
            response['${p}Enable'] == true || response['${p}Enable'] == 'true',
        description: (response['${p}Description'] ?? '') as String,
        triggerPort: int.tryParse(response['${p}Port']?.toString() ?? '') ?? 0,
        triggerPortEndRange:
            int.tryParse(response['${p}PortEndRange']?.toString() ?? '') ?? 0,
        triggerProtocol: (response['${p}Protocol'] ?? '') as String,
        rules: rules,
      ));
    }
    return PortTriggering(items: items);
  }

  /// Update a single instance via USP Set message
  static Future<void> update(
      UspService client, PortTriggerUpdate update) async {
    final params = <String, dynamic>{};
    if (update.enabled != null)
      params['${update.instancePath}Enable'] = update.enabled;
    if (update.description != null)
      params['${update.instancePath}Description'] = update.description;
    if (update.triggerPort != null)
      params['${update.instancePath}Port'] = update.triggerPort;
    if (update.triggerPortEndRange != null)
      params['${update.instancePath}PortEndRange'] = update.triggerPortEndRange;
    if (update.triggerProtocol != null)
      params['${update.instancePath}Protocol'] = update.triggerProtocol;
    if (params.isNotEmpty) await client.set(params);
  }

  /// Update multiple instances in a single USP Set message
  static Future<void> updateMany(
      UspService client, List<PortTriggerUpdate> updates,
      {bool allowPartial = false}) async {
    final params = <String, dynamic>{};
    for (final update in updates) {
      if (update.enabled != null)
        params['${update.instancePath}Enable'] = update.enabled;
      if (update.description != null)
        params['${update.instancePath}Description'] = update.description;
      if (update.triggerPort != null)
        params['${update.instancePath}Port'] = update.triggerPort;
      if (update.triggerPortEndRange != null)
        params['${update.instancePath}PortEndRange'] =
            update.triggerPortEndRange;
      if (update.triggerProtocol != null)
        params['${update.instancePath}Protocol'] = update.triggerProtocol;
    }
    if (params.isNotEmpty) await client.set(params, allowPartial: allowPartial);
  }

  /// Add a new PortTrigger instance via USP Add message
  static Future<String> add(
    UspService client, {
    bool? enabled,
    String? description,
    int? triggerPort,
    int? triggerPortEndRange,
    String? triggerProtocol,
  }) async {
    final params = <String, dynamic>{};
    if (enabled != null) params['Enable'] = enabled;
    if (description != null) params['Description'] = description;
    if (triggerPort != null) params['Port'] = triggerPort;
    if (triggerPortEndRange != null)
      params['PortEndRange'] = triggerPortEndRange;
    if (triggerProtocol != null) params['Protocol'] = triggerProtocol;
    return await client.add('Device.NAT.PortTrigger.', params);
  }

  /// Delete a PortTrigger instance via USP Delete message
  static Future<void> delete(UspService client, String instancePath) async {
    await client.delete(instancePath);
  }

  /// Add a new PortTriggerForwardRule instance under a parent instance
  static Future<String> addPortTriggerForwardRule(
    UspService client,
    String parentInstancePath, {
    int? forwardPort,
    int? forwardPortEndRange,
    String? forwardProtocol,
  }) async {
    final params = <String, dynamic>{};
    if (forwardPort != null) params['Port'] = forwardPort;
    if (forwardPortEndRange != null)
      params['PortEndRange'] = forwardPortEndRange;
    if (forwardProtocol != null) params['Protocol'] = forwardProtocol;
    return await client.add('${parentInstancePath}Rule.', params);
  }

  /// Delete a PortTriggerForwardRule instance via USP Delete message
  static Future<void> deletePortTriggerForwardRule(
      UspService client, String instancePath) async {
    await client.delete(instancePath);
  }
}
