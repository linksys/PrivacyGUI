// AUTO-GENERATED CODE - DO NOT EDIT
// This file was generated by usp-codegen
// Any modifications will be overwritten on next generation

import 'package:privacy_gui/core/usp/services/usp_service.dart';

/// Single instance from WiFiAccessPoints
class WiFiAccessPoint {
  final String instancePath;
  final bool enable;
  final String status;
  final String modesSupported;
  final String securityModeEnabled;
  final String encryptionMode;
  final String keyPassphrase;
  final bool ssidAdvertisementEnabled;
  final String ssidReference;

  const WiFiAccessPoint({
    required this.instancePath,
    required this.enable,
    required this.status,
    required this.modesSupported,
    required this.securityModeEnabled,
    required this.encryptionMode,
    required this.keyPassphrase,
    required this.ssidAdvertisementEnabled,
    required this.ssidReference,
  });
}

/// Update descriptor for WiFiAccessPoint instances
class WiFiAccessPointUpdate {
  final String instancePath;
  final String? securityModeEnabled;
  final String? keyPassphrase;
  final bool? ssidAdvertisementEnabled;

  const WiFiAccessPointUpdate({
    required this.instancePath,
    this.securityModeEnabled,
    this.keyPassphrase,
    this.ssidAdvertisementEnabled,
  });
}

/// WiFi access point status and security
class WiFiAccessPoints {
  final List<WiFiAccessPoint> items;

  const WiFiAccessPoints({required this.items});

  static const _paths = [
    'Device.WiFi.AccessPoint.*.Enable',
    'Device.WiFi.AccessPoint.*.Status',
    'Device.WiFi.AccessPoint.*.Security.ModesSupported',
    'Device.WiFi.AccessPoint.*.Security.ModeEnabled',
    'Device.WiFi.AccessPoint.*.Security.EncryptionMode',
    'Device.WiFi.AccessPoint.*.Security.KeyPassphrase',
    'Device.WiFi.AccessPoint.*.SSIDAdvertisementEnabled',
    'Device.WiFi.AccessPoint.*.SSIDReference',
  ];

  /// Fetch all instances via USP Get message
  static Future<WiFiAccessPoints> fetch(UspService client) async {
    final response = await client.get(_paths);
    return WiFiAccessPoints._fromResponse(response);
  }

  factory WiFiAccessPoints._fromResponse(Map<String, dynamic> response) {
    final items = <WiFiAccessPoint>[];
    const basePath = 'Device.WiFi.AccessPoint.';
    final ids = <String>{};
    for (final key in response.keys) {
      if (key.startsWith(basePath)) {
        final rest = key.substring(basePath.length);
        final dot = rest.indexOf('.');
        if (dot > 0) ids.add(rest.substring(0, dot));
      }
    }
    final sorted = ids.toList()
      ..sort((a, b) => (int.tryParse(a) ?? 0).compareTo(int.tryParse(b) ?? 0));
    for (final id in sorted) {
      final p = '$basePath$id.';
      if ([
        response['${p}Enable'],
        response['${p}Status'],
        response['${p}Security.ModesSupported'],
        response['${p}Security.ModeEnabled'],
        response['${p}Security.EncryptionMode'],
        response['${p}Security.KeyPassphrase'],
        response['${p}SSIDAdvertisementEnabled'],
        response['${p}SSIDReference']
      ].every((v) =>
          v == null ||
          v == '' ||
          v == '0' ||
          v == 0 ||
          v == false ||
          v == 'false')) continue;
      items.add(WiFiAccessPoint(
        instancePath: p,
        enable: response['${p}Enable'] == true ||
            response['${p}Enable'] == 'true' ||
            response['${p}Enable'] == '1',
        status: (response['${p}Status'] ?? '') as String,
        modesSupported:
            (response['${p}Security.ModesSupported'] ?? '') as String,
        securityModeEnabled:
            (response['${p}Security.ModeEnabled'] ?? '') as String,
        encryptionMode:
            (response['${p}Security.EncryptionMode'] ?? '') as String,
        keyPassphrase: (response['${p}Security.KeyPassphrase'] ?? '') as String,
        ssidAdvertisementEnabled:
            response['${p}SSIDAdvertisementEnabled'] == true ||
                response['${p}SSIDAdvertisementEnabled'] == 'true' ||
                response['${p}SSIDAdvertisementEnabled'] == '1',
        ssidReference: (response['${p}SSIDReference'] ?? '') as String,
      ));
    }
    return WiFiAccessPoints(items: items);
  }

  /// Update a single instance via USP Set message
  static Future<void> update(
      UspService client, WiFiAccessPointUpdate update) async {
    final params = <String, dynamic>{};
    if (update.securityModeEnabled != null)
      params['${update.instancePath}Security.ModeEnabled'] =
          update.securityModeEnabled;
    if (update.keyPassphrase != null)
      params['${update.instancePath}Security.KeyPassphrase'] =
          update.keyPassphrase;
    if (update.ssidAdvertisementEnabled != null)
      params['${update.instancePath}SSIDAdvertisementEnabled'] =
          update.ssidAdvertisementEnabled;
    if (params.isNotEmpty) await client.set(params);
  }

  /// Update multiple instances in a single USP Set message
  static Future<void> updateMany(
      UspService client, List<WiFiAccessPointUpdate> updates,
      {bool allowPartial = false}) async {
    final params = <String, dynamic>{};
    for (final update in updates) {
      if (update.securityModeEnabled != null)
        params['${update.instancePath}Security.ModeEnabled'] =
            update.securityModeEnabled;
      if (update.keyPassphrase != null)
        params['${update.instancePath}Security.KeyPassphrase'] =
            update.keyPassphrase;
      if (update.ssidAdvertisementEnabled != null)
        params['${update.instancePath}SSIDAdvertisementEnabled'] =
            update.ssidAdvertisementEnabled;
    }
    if (params.isNotEmpty) await client.set(params, allowPartial: allowPartial);
  }
}
