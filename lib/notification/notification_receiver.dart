import 'dart:async';
import 'dart:convert';


void pushNotificationHandler(Map<String, dynamic> data) {
  // Smart device token
  if (data.containsKey('token')) {
    final token = data['token'];
  } else {

  }
}