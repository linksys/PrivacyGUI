import 'package:flutter/material.dart';

class MoabRadius {
  static const xs = 4.0;
  static const s = 8.0;
  static const m = 20.0;
  static const l = 50.0;
  static const xl = 100.0;

  static BorderRadius getXS() => BorderRadius.circular(xs);
  static BorderRadius getS() => BorderRadius.circular(s);
  static BorderRadius getM() => BorderRadius.circular(m);
  static BorderRadius getL() => BorderRadius.circular(l);
  static BorderRadius getXL() => BorderRadius.circular(xl);
}
